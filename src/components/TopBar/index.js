import * as Styled from "./style";

import TopBarUser from "../TopBarUser";

const TopBar = (props) => {
  return (
    <Styled._ attr="main">
      <TopBarUser />
    </Styled._>
  );
};

export default TopBar;
